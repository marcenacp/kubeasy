const assert = require('assert');

describe('[HOOKS] Pods', () => {
  describe('formatData', () => {
    it('should extract status', () => {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});
